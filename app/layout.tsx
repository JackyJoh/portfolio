import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Jackson Johannessen | CS Portfolio',
  description: 'Computer Science student building scalable systems, full-stack applications, and data-driven solutions. UF, Rocket Design Team, Data Analysis.',
  generator: 'v0.app',
  icons: {
    icon: '/jj_logo.png',
    apple: '/jj_logo.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
